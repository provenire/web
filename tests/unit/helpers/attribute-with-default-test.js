import { attributeWithDefault } from 'web/helpers/attribute-with-default';
import { module, test } from 'qunit';

module('Unit | Helper | attribute with default');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = attributeWithDefault([42]);
  assert.ok(result);
});
