import notification from 'web/utils/notification';
import { module, test } from 'qunit';

module('Unit | Utility | notification');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = notification();
  assert.ok(result);
});
